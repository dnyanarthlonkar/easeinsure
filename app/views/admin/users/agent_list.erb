
<table width="100%" border="0" cellspacing="0" cellpadding="0">
  <tr>
    <td>
      <%= link_to "New Agent", new_admin_user_path(type: 'agent') %>
    </td>
  </tr>
</table>
</div>
</div>
</br>
<section id="tables">
<table border='1'>
<thead>
  <tr>
    <th>User Name</th>
    <th>First name</th>
    <th>Last Name</th>
    <th>Email</th>
    <th>Role</th>
    <th>Actions</th>
  </tr>
</thead>
    <% @users.each do |user,index |  %>
        <tbody>
          <tr>
            <td><%= user.try(:username) %></td>
            <td><%= user.try(:first_name) %></td>
            <td><%= user.try(:last_name) %></td>
            <td><%= user.try(:email) %></td>
            <td><%= user.roles.first.try(:name) %></td>
              <td><%= link_to "Edit", edit_admin_user_path(user) , :class => "btn"%>
              | <%= link_to "Delete", admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        </tbody>
    <% end %> 
</table>
                 
              