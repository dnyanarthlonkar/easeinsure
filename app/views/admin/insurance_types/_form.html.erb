<%= form_for([:admin,insurance_type]) do |f| %>
  <% if insurance_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insurance_type.errors.count, "error") %> prohibited this insurance_type from being saved:</h2>

      <ul>
      <% insurance_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.cktext_area :description %>
  </div>

  <div class="field">
    <%= f.label :parent_id %>
    <%= f.select :parent_id, InsuranceType.roots.pluck(:name,:id), {:include_blank => "Parent"} %>
  </div>

  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active, :checked => true %>
  </div>  

  <div class="actions">
    <%= f.submit "Submit" ,class: "btn btn-primary" %>
    <%= link_to 'Cancel', admin_insurance_types_path, class: 'btn btn-danger' %>
  </div>
<% end %>
